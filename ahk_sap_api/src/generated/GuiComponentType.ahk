#Requires AutoHotkey v2.0

class GuiComponentType {
    ; Numeric values are version-sensitive across SAP GUI releases.
    ; Keep this list conservative and use symbolic names when possible.
    static Unknown := -1
}
